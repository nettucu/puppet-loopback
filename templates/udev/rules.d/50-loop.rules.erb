<% @content['files'].each do |f, sz| %>
#f=<%= f %>, sz=<%= sz %>
#echo "losetup -P /dev/loop<%= @fileno %> <%= f %>"
KERNEL=="loop<%= @fileno %>", SYMLINK+="asm-data<%= @fileno %>"
<% @fileno = @fileno + 1 %>
<% end %>
