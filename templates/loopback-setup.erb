#!/bin/bash

echo "loopback-setup"
echo base_dir=<%= @base_dir %>
<% @content['files'].each do |f, sz| %>
  echo "f=<%= f %>, sz=<%= sz %>"
  losetup -P /dev/loop<%= @fileno %> <%= @base_dir %>/<%= f %>
  <% @fileno = @fileno + 1 %>
<% end %>
