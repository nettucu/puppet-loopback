#!/bin/bash

echo "loopback-setup"
echo base_dir=<%= @base_dir %>
<% @content['files'].each do |f, sz| %>
  echo "f=<%= f %>, sz=<%= sz %>"
  echo "losetup -P /dev/loop<%= @fileno %>"
  <% @fileno = @fileno + 1 %>
<% end %>
